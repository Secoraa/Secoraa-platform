# Web Scanner

Pure Python web security scanner that performs passive checks and outputs
structured JSON findings.

## Usage

```
python -m app.scanners.web_scanner.main example.com --depth 2 --pages 50 --timeout 5 --output report.json
```
