# Railway Python App Configuration
[build]
builder = "nixpacks"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/api/v1alpha1/backend/docs"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10