# Minimal dependencies for the Secoraa API scanner CLI (CI/CD image).
# Excludes sqlalchemy, psycopg2, minio, fpdf2, celery, pandas, numpy — none
# of those are needed when `run_api_scan()` is called with db=None.

# --- HTTP / Networking ---
requests==2.32.5
httpx==0.28.1
aiohttp==3.10.11
aiodns==3.6.1
aiohappyeyeballs==2.6.1
multidict==6.7.0
yarl==1.22.0
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.2
certifi==2026.1.4
dnspython==2.6.1
beautifulsoup4==4.12.3

# --- Spec parsing / utils ---
PyYAML==6.0.3
pydantic==2.12.5
pydantic_core==2.41.5
typing_extensions==4.15.0
anyio==4.12.0
h11==0.16.0

# --- Crypto (JWT tests) ---
PyJWT==2.10.1
pycryptodome==3.23.0

# --- Retry / logging ---
tenacity==9.1.2
rich==14.2.0
